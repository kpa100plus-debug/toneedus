{
  "name": "modu-challenge",
  "version": "0.5.1",
  "private": true,
  "type": "module",
  "description": "모두의 챌린지 - 범용 성과형 챌린지 마켓플레이스 WEB 운영형 베타",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "prepare:source": "node scripts/prepare.mjs",
    "deploy": "npm run prepare:source && wrangler d1 migrations apply DB --remote && wrangler deploy",
    "dev": "npm run prepare:source && wrangler dev --local --persist-to=.wrangler/state"
  },
  "cloudflare": {
    "bindings": {
      "ADMIN_BOOTSTRAP_TOKEN": {
        "description": "최초 최고관리자 생성용 일회성 비밀값입니다. 영문·숫자·기호를 섞어 32자 이상 입력하세요."
      },
      "PAYMENT_WEBHOOK_SECRET": {
        "description": "PG 계약 전에는 임의의 32자 이상 비밀값을 입력하세요. 실제 결제 활성화 때 PG Webhook 서명값으로 교체합니다."
      },
      "PAYOUT_WEBHOOK_SECRET": {
        "description": "지급대행 계약 전에는 임의의 32자 이상 비밀값을 입력하세요. 실제 보상금 지급 활성화 때 지급대행 Webhook 서명값으로 교체합니다."
      }
    }
  },
  "devDependencies": {
    "wrangler": "4.126.0"
  }
}
